/**
 * @fileoverview UserRole - Enum for user roles in the system
 * @summary Defines all possible user roles
 * @description Enumeration of user roles used throughout the application
 * for role-based access control and authorization.
 */

/**
 * User roles in the system
 */
export enum UserRole {
  Admin = 'Admin',
  Supervisor = 'Supervisor',
  PSO = 'PSO',
  ContactManager = 'ContactManager',
  SuperAdmin = 'SuperAdmin',
}

