/**
 * @fileoverview TimerColor enum
 * @summary Enum for timer colors
 * @description Colors for timer display based on time remaining
 */

export enum TimerColor {
  GREEN = 'green',
  YELLOW = 'yellow',
  RED = 'red',
}

