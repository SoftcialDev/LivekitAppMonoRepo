/**
 * @fileoverview StreamingStatus enum
 * @summary Enum representing streaming status types
 * @description Enum representing the different streaming status states
 */

/**
 * Enum representing streaming status types
 */
export enum StreamingStatus {
  ON_BREAK = 'on_break',
  DISCONNECTED = 'disconnected',
  OFFLINE = 'offline',
}

