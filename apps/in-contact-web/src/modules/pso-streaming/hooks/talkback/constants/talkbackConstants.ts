/**
 * @fileoverview Talkback constants
 * @summary Constants for talkback functionality
 * @description Constants for countdown duration and intervals
 */

/**
 * Countdown duration in milliseconds before activating talkback
 */
export const COUNTDOWN_DURATION_MS = 3000;

/**
 * Countdown interval in milliseconds (how often to update countdown display)
 */
export const COUNTDOWN_INTERVAL_MS = 1000;

/**
 * Maximum number of attempts to wait for room connection
 */
export const MAX_ROOM_WAIT_ATTEMPTS = 10;

/**
 * Interval between room connection checks in milliseconds
 */
export const ROOM_WAIT_INTERVAL_MS = 100;

/**
 * Additional wait time for room connection after initial checks
 */
export const ROOM_CONNECTION_WAIT_MS = 500;

