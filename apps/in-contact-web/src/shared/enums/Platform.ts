/**
 * @fileoverview Platform - Enum for platform types
 * @summary Defines platform identifier values
 * @description Enum representing the different platform types (Electron app or browser)
 */

/**
 * Enum representing platform types in the system
 * @description Defines the possible platforms where the application can run
 */
export enum Platform {
  Electron = "electron",
  Browser = "browser"
}

