/**
 * @fileoverview RecordingStartStatus - Enum for recording start operation status
 * @summary Defines recording start status values
 * @description Enum representing the different statuses when starting a recording session
 */

/**
 * Enum representing recording start operation status
 * @description Defines the possible statuses when a recording is started
 */
export enum RecordingStartStatus {
  /** Recording started successfully */
  Started = "STARTED",
  /** Recording failed to start */
  Failed = "FAILED"
}

