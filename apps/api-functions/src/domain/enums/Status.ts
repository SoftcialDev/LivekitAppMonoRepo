/**
 * @fileoverview Status - Enum for presence status
 * @summary Defines presence status values matching Prisma schema
 * @description Enum representing the different presence statuses in the system
 */

/**
 * Enum representing presence status in the system
 * @description Matches the Status enum in Prisma schema
 */
export enum Status {
  Online = "online",
  Offline = "offline"
}
