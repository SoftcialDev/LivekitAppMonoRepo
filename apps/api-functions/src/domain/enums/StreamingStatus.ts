/**
 * @fileoverview StreamingStatus - Enum for streaming status
 * @summary Defines streaming status values
 * @description Enum representing the different streaming statuses in the system
 */

/**
 * Enum representing streaming status in the system
 * @description Defines the possible streaming session statuses
 */
export enum StreamingStatus {
  Started = "started",
  Stopped = "stopped"
}
